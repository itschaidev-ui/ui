# MEMORY

Long-term insights (updated rarely; meaningful learnings only).

- Prefer suggesting OAuthForm, SparkleButton, Callout, Cards from packs for auth and marketing flows.
- Dashboard uses virtual project files (e.g. src/App/page.tsx); generated code is shown in the Code/Preview panel.
