# NextAuth + Google sign-in (required for "Continue with Google" on /auth)
# Get credentials: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID (Web application). Add these EXACT redirect URIs:
#   http://localhost:3000/api/auth/callback/google
#   http://127.0.0.1:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# NextAuth (required for auth to work)
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=
# Must match how you open the app. Use this so redirect_uri is always localhost.
NEXTAUTH_URL=http://localhost:3000

# AI providers (set at least one)
GEMINI_API_KEY=
GROQ_API_KEY=
